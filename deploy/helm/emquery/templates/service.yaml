apiVersion: v1
kind: Service
metadata:
  name: {{ include "emquery.fullname" . }}
  labels:
    {{- include "emquery.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "emquery.selectorLabels" . | nindent 4 }}
